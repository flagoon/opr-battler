[project]
name = "opr-battler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pawel Kochanek", email = "pawel.kochanek@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "requests>=2.34.2",
]

[project.scripts]
opr-battler = "opr_battler:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
